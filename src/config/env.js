import { config } from 'dotenv';

const envFile = `.env.${process.env.NODE_ENV || 'development'}`;
config({ path: envFile });

export const {
  PORT,
  NODE_ENV,
  DATABASE_URL,
  DATABASE_NAME,
  JWT_SECRET,
  JWT_EXPIRES_IN,
  ARCJET_KEY,
  LOG_LEVEL,
  NEON_API_KEY,
  NEON_PROJECT_ID,
  PARENT_BRANCH_ID,
  DELETE_BRANCH,
  REDIS_URL,
  REDIS_HOST,
  REDIS_PORT,
  REDIS_PASSWORD,
  CACHE_TTL_DEFAULT,
  CACHE_TTL_SHORT,
  CACHE_TTL_LONG,
  EMAIL_PASSWORD,
  ACCOUNT_EMAIL,
  NGROK_AUTH_TOKEN,
  HUME_API_KEY,
  HUME_SECRET_KEY,
  HUME_CONFIG_ID,
  TWILIO_ACCOUNT_SID,
  TWILIO_AUTH_TOKEN,
  TWILIO_PHONE_NUMBER,
  TWILIO_WEBHOOK_URL,
  OPENAI_API_KEY,
  GOOGLE_CLIENT_ID,
  GOOGLE_CLIENT_SECRET,
  GOOGLE_REDIRECT_URI,
  FRONTEND_URL,
} = process.env;
